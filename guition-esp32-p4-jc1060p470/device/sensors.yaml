globals:
  - id: selected_light
    type: std::string
    restore_value: false
    initial_value: '""'
  - id: selected_light_name
    type: std::string
    restore_value: false
    initial_value: '""'
  - id: selected_light_is_dimmable
    type: bool
    restore_value: false
    initial_value: "false"
  - id: selected_light_is_switch
    type: bool
    restore_value: false
    initial_value: "false"
  - id: selected_light_valid
    type: bool
    restore_value: false
    initial_value: "false"
  - id: tile_long_press_active
    type: bool
    restore_value: false
    initial_value: "false"

script:
  - id: reset_light_context
    then:
      - lambda: |-
          id(selected_light).clear();
          id(selected_light_name).clear();
          id(selected_light_is_dimmable) = false;
          id(selected_light_is_switch) = false;
          id(selected_light_valid) = false;
          id(tile_long_press_active) = false;
      - lvgl.label.update:
          id: selected_light_title
          text: "Aucune sélection"
      - lvgl.label.update:
          id: selected_light_brightness
          text: "Lum : --"
      - lvgl.widget.update:
          id: quick_25_button
          state: { disabled: true }
      - lvgl.widget.update:
          id: quick_50_button
          state: { disabled: true }
      - lvgl.widget.update:
          id: quick_100_button
          state: { disabled: true }

  - id: select_light_context
    parameters:
      entity_id: string
      display_name: string
      is_dimmable: bool
      is_switch: bool
    then:
      - lambda: |-
          id(selected_light) = entity_id;
          id(selected_light_name) = display_name;
          id(selected_light_is_dimmable) = is_dimmable;
          id(selected_light_is_switch) = is_switch;
          id(selected_light_valid) = true;
      - lvgl.label.update:
          id: selected_light_title
          text:
            format: "%s"
            args: [ display_name.c_str() ]
      - if:
          condition:
            lambda: 'return is_dimmable;'
          then:
            - lvgl.widget.update: { id: quick_25_button, state: { disabled: false } }
            - lvgl.widget.update: { id: quick_50_button, state: { disabled: false } }
            - lvgl.widget.update: { id: quick_100_button, state: { disabled: false } }
            - lvgl.label.update: { id: selected_light_brightness, text: "Lum : --" }
          else:
            - lvgl.widget.update: { id: quick_25_button, state: { disabled: true } }
            - lvgl.widget.update: { id: quick_50_button, state: { disabled: true } }
            - lvgl.widget.update: { id: quick_100_button, state: { disabled: true } }
            - lvgl.label.update: { id: selected_light_brightness, text: "Lum : N/A" }

  - id: toggle_selected_light
    then:
      - if:
          condition:
            lambda: 'return id(selected_light_valid);'
          then:
            - homeassistant.service:
                service: !lambda |-
                  return id(selected_light_is_switch) ? "switch.toggle" : "light.toggle";
                data:
                  entity_id: !lambda 'return id(selected_light);'

  - id: set_selected_light_brightness
    parameters:
      brightness_pct: int
    then:
      - if:
          condition:
            lambda: 'return id(selected_light_valid) && id(selected_light_is_dimmable) && !id(selected_light_is_switch);'
          then:
            - homeassistant.service:
                service: light.turn_on
                data:
                  entity_id: !lambda 'return id(selected_light);'
                  brightness_pct: !lambda 'return brightness_pct;'
binary_sensor:
  # Bureau
  - platform: homeassistant
    id: bureau_light_state
    entity_id: light.ampoule_bureau
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_scene_features_button
          state:
            checked: !lambda 'return x;'

  # Fado bureau
  - platform: homeassistant
    id: fado_bureau_light_state
    entity_id: light.0x001788010dc8ba13
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_scene_calls_button
          state:
            checked: !lambda 'return x;'

  # Chambre
  - platform: homeassistant
    id: chambre_light_state
    entity_id: light.0x001788010c014263
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_scene_evening_button
          state:
            checked: !lambda 'return x;'

  # Led chambre
  - platform: homeassistant
    id: led_chambre_light_state
    entity_id: light.0x001788010c58b8f2
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_scene_dimmed_button
          state:
            checked: !lambda 'return x;'

  # Salon
  - platform: homeassistant
    id: salon_light_state
    entity_id: light.0x00178801062d1c11
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_blind_open_button
          state:
            checked: !lambda 'return x;'

  # Salle à manger
  - platform: homeassistant
    id: salle_a_manger_light_state
    entity_id: light.0x001788010dc8c061
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_blind_66_button
          state:
            checked: !lambda 'return x;'

  # La boule
  - platform: homeassistant
    id: la_boule_light_state
    entity_id: light.la_boule
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_blind_33_button
          state:
            checked: !lambda 'return x;'

  # Salle de bain
  - platform: homeassistant
    id: salle_de_bain_light_state
    entity_id: light.0x001788010c0adbc8
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_blind_close_button
          state:
            checked: !lambda 'return x;'

  # Cuisine
  - platform: homeassistant
    id: cuisine_light_state
    entity_id: light.0x001788010c2dee03
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: printer_power_button
          state:
            checked: !lambda 'return x;'

  # Led cuisine
  - platform: homeassistant
    id: led_cuisine_light_state
    entity_id: light.0x001788010d0b3aa9
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: filament_heater_button
          state:
            checked: !lambda 'return x;'

  # Couloir
  - platform: homeassistant
    id: couloir_light_state
    entity_id: light.0x001788010c2de07e
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: office_aircon_button
          state:
            checked: !lambda 'return x;'

  # Lampe couloir (prise)
  - platform: homeassistant
    id: lampe_couloir_switch_state
    entity_id: switch.lampe_couloir
    trigger_on_initial_state: true
    on_state:
      - lvgl.widget.update:
          id: battery_charger_button
          state:
            checked: !lambda 'return x;'


sensor:
  # Température intérieure
  - platform: homeassistant
    id: indoor_temperature
    entity_id: sensor.office_temperature
    internal: true
    on_value:
      - lvgl.label.update:
          id: temperatures
          text:
            format: "%.0f° / -°"
            args:
              - x

  # Température extérieure
  - platform: homeassistant
    id: outdoor_temperature
    entity_id: sensor.outdoor_temperature
    internal: true
    on_value:
      - lvgl.label.update:
          id: temperatures
          text:
            format: "%.0f° / %.0f°"
            args:
              - id(indoor_temperature).state
              - x

  # Bureau - luminosité réelle
  - platform: homeassistant
    id: bureau_brightness
    entity_id: light.ampoule_bureau
    attribute: brightness
    internal: true
    filters:
      - multiply: 0.3921568627
    on_value:
      - lvgl.label.update:
          id: selected_light_brightness
          text:
            format: "Lum : %.0f%%"
            args:
              - x


text_sensor:
  # Heure
  - platform: homeassistant
    id: current_time_ha
    entity_id: sensor.time
    internal: true
    on_value:
      - lvgl.label.update:
          id: display_time
          text:
            format: "%s"
            args:
              - x.c_str()


